// Calls main::main(x, y);
module main {
    fn main(x: i32, y: i32): i32 {
        x + y
    }
}

struct Point {
    x: i32,
    y: i32,

    fn getX(&this): i32 {
        this.x
    }
}

fn getPoint(x: i32, y: i32): Point {
    Point {
        x: x,
        y: y,
    }
}
